things to fix:

LaTeX files should automatically have
non-ASCII accented characters converted
to good LaTeX macros.

LaTeX files should automatically have
long line broken into pieces length<99
(starting with after each \\)

